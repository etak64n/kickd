# Builds kickd for every supported platform when a version tag is pushed,
# and publishes the executables with their SHA-256 checksums as a release.
name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # Release builds use the latest Go release, so they include its
      # security fixes; go.mod only sets the minimum version.
      - uses: actions/setup-go@v7
        with:
          go-version: stable
          check-latest: true

      - name: Test
        run: go test ./...

      - name: Build
        run: ./scripts/build-all.sh "$GITHUB_REF_NAME"

      # The executables include the Go standard library and third-party Go
      # modules, whose licenses ask for their texts to travel with them.
      - name: Licenses
        run: |
          cp LICENSE dist/LICENSE
          ./scripts/third-party-licenses.sh > dist/THIRD_PARTY_LICENSES.txt

      - name: Checksums
        working-directory: dist
        run: sha256sum kickd-* LICENSE THIRD_PARTY_LICENSES.txt > checksums.txt

      - name: Publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --title "kickd $GITHUB_REF_NAME" --generate-notes --verify-tag
